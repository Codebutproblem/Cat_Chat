extends ../../layouts/default.pug
block style 
    link(rel="stylesheet", href="/client/css/friend.css")
block main
    .main 
        div(user-id=user.id)
        h1 Tìm kiếm bạn bè
        .friend 
            .inner-search
                form(action="" class="form-search")
                    input(type="text" name="keyword" class="input-search-user" id="input-search-user" placeholder="Tìm kiếm...")
                    button(type="submit") 
                        i(class="fa-solid fa-magnifying-glass") 

            .inner-users
                .add-friend-box.row.align-items-stretch
                    each user in users 
                        .col-md-4
                            .box-user 
                                .inner-avatar 
                                    img(src=user.avatar)
                                .inner-info 
                                    div(class="inner-name") #{user.fullName}
                                    - const classButton = "inner-button " + user.classBtn
                                    div(class=classButton data-user-id=user.id) 
                                        button(class="add-btn btn btn-sm btn-primary") Kết bạn
                                        button(class="accept-btn btn btn-sm btn-success") Đồng ý
                                        button(class="refuse-btn btn btn-sm btn-danger ms-2") Từ chối
                                        button(class="cancel-btn btn btn-sm btn-secondary") Hủy
                                        button(class="unfriend-btn btn btn-sm btn-danger") Hủy kết bạn
                                    
block script     
    script(src="/client/js/friend.js") 
            